# hoshibiyori

星景写真撮影に特化した天気予報AIプロジェクト。

## プロジェクトの独立性

- 姉妹プロジェクト「yamabiyori」（`C:\mountain-weather\`、GitHub公開済み）とは**完全に独立したリポジトリ**である。
- yamabiyori側のコードは参照・変更しないこと。コンセプトが似ていても、コードやライブラリ構成を流用・コピーしない。
- 原則として、何かを調べる必要がある場合でも `C:\mountain-weather\` 以下のファイルを開いたり編集したりしない。
  - **例外(2026-09-05、ユーザー承認済み)**: `areas.json`(おすすめエリア機能)の作成にあたり、
    yamabiyori側の山域名称・境界データを**読み取り専用**で一次参照し、値のみをhoshibiyori独自の
    `areas.json`に複製することをユーザーが明示的に許可した。コードのimport/参照は引き続き禁止で、
    「値をコピーして独自ファイル化する」用途に限る。今後も同様の必要が生じた場合は、
    このケースを前例としつつ、その都度ユーザーに確認すること(黙って恒常的な参照ルールに
    拡大しない)。yamabiyori側のファイルへの書き込み・変更は今回も行っていない。
  - **例外(2026-09-06、ユーザー承認済み)**: `step5_cloud_forecast/jma_weather_map.py`
    (気象庁地上天気図のスクリーンショット取得)の実装にあたり、yamabiyori側
    (`mountain_weather_detail.py` の `fetch_jma_weather_map()`)を**読み取り専用**で
    一次参照し、「JMAの天気図ページは静的URLがなくJSで動的生成されるためPlaywrightで
    `<img>`要素をスクリーンショットする必要がある」「予想天気図は『ひとつ後の時間を
    表示』ボタンで+24h・+48hの2段階しか進めない」という技術的制約・仕様のみを把握した。
    実際のCSSセレクタ等の実装はコピーせず、対象ページのDOM・JSソースを実機で
    独自に調査して新規実装している。今後も同様の必要が生じた場合は、この
    ケースを前例としつつ、その都度ユーザーに確認すること。
  - **例外(2026-09-13、ユーザー承認済み)**: ユーザーから「hoshibiyoriの天気予報ロジックを
    yamabiyoriで採用したものと同じにしてほしい(もともと同じロジックだったがyamabiyori側が
    大幅に改善された)」との明示的な依頼があり、yamabiyori側の`mountain_weather_core.py`
    (`fetch_open_meteo`・標高補間レイヤー`interp_at_altitude`等)・`mountain_weather_mvp.py`
    (`fetch_forecast`のMSM/ECMWF結合)・`CHANGELOG.md`・`skills/yamabiyori/SKILL.md`
    (「共通のデータ取得ロジック」「生JSONキャッシュ層」「既知の落とし穴」節)を**読み取り専用**で
    参照し、設計上の知見のみを`step5_cloud_forecast/elevation_cloud.py`に独自実装として
    取り込んだ(詳細は「標高考慮の雲量判定」セクション)。コードのコピー・import、
    yamabiyori側への書き込みはしていない。今後も同様の必要が生じた場合は、この
    ケースを前例としつつ、その都度ユーザーに確認すること。

## 観測者に寄り添う — hoshibiyori(星空撮影者目線)とyamabiyori(登山者目線)の違い(2026-09-13明文化)

両プロジェクトは同じデータ源(Open-Meteo経由のJMA MSM+ECMWF IFS)と同じ取得・結合・キャッシュの
骨格を持つが、**「観測者が誰で、何を見上げ/見渡すか」が違うため、雲の見方・評価方法は意図的に
異なる**。ユーザーの方針は「観測者に寄り添う」こと。yamabiyoriの改善を取り込む際も、
登山者向けの評価軸をそのまま持ち込まず、星空撮影者にとって何が正解かを毎回問い直すこと。

| 観点 | hoshibiyori(星空撮影者) | yamabiyori(登山者) |
|---|---|---|
| 観測者の位置 | 地表(海岸・湖畔・高原)が大半。山上は一部 | 稜線・山頂(標高1000〜3000m級) |
| 見たいもの | **頭上の空**。空のどれだけが雲に覆われるか(天の川・星が見えるか) | **自分の周りの空気**。稜線がガスに巻かれるか、濡れるか、風は強いか |
| 正解に近い変数 | モデル本来の全層雲量`cloud_cover`(下層/中層/上層の重なりをモデル自身が考慮済み) | 山頂標高へ補間した気圧面の湿度・雲量(空気の飽和度=ガスの代理指標)、降水量、風速 |
| 気圧面別雲量の役割 | **観測者より下の雲を差し引く**ためだけに使う(山上の地点で雲海の上に出られるか) | 主役。山頂〜稜線帯の湿潤層判定(雨天・ガス判定)に直接使う |
| 薄い上層雲(巻雲) | **致命的**。天の川のコントラストが消える。上層雲量を必ず評価する | ほぼ無視できる(展望にはやや影響、安全には無関係) |
| 下層雲(層雲・霧) | 低標高地点では頭上を塞ぐ主犯。山上地点では「雲海」として撮影対象にもなる | 稜線より下なら雲海(好条件)、稜線にかかればガス(悪条件) |
| 時間帯 | 天文薄明終了〜開始の夜間、日付をまたぐ | 日の出〜14時前後のメインタイム、日没まで |
| 悪条件の性質 | 「見えない」だけで危険は無い → 過大評価(空振り)も過小評価(見逃し)も等しくコスト | 危険に直結 → 見逃しの方がコストが高く、悲観側に寄せる設計が正当化される |
| 降水・風 | スコアには入れない(雲で十分に代表される。風は機材の安定性の問題で別軸) | スコアの主軸(降水50%)、強風は危険信号 |
| 光害・月 | 主軸(天の川の見え方を決める) | 無関係 |

**この違いが実装に与えた影響(2026-09-13)**: yamabiyoriの気圧面ベースの設計を取り込んだ直後、
ユーザーから「星空撮影者は地表から見上げるので雲の見方が違うはず」と指摘があり点検した結果、
hoshibiyoriの視界遮蔽雲量は気圧面別(相対湿度近似)雲量を全層ランダム重ね合わせしており、
1枚の雲を複数の独立した層として何重にも数えて**全層雲量に対し平均+15.6pt過大**、30%閾値で
低標高6地点×72時間のうち46時間を誤って「曇り」と判定していた(登山者向きの「悲観側に寄せる」
性質を、それが正当化されない星空用途に持ち込んでいた)。修正内容は「標高考慮の雲量判定」
セクション参照。**yamabiyori側の湿潤層ルール・強制上昇ルール・降水判定などは登山者の安全のための
評価軸であり、hoshibiyoriには持ち込まない。**

## 開発の進め方

- 開発は複数ステップに分かれている。**全体を一度に作ろうとせず、各ステップごとに実現性を確認しながら進めること。**
- 現在のフェーズ(2026-09-06時点): **ステップ1〜7まで実装・検証済み。ステップ8以降は未着手。**
  - ステップ1(`step1_astronomy/`): 天体（星、天の川、月など）の位置計算。Skyfieldベース。実装済み・妥当性確認済み。
  - ステップ2(`step2_terrain/`): 観測地点周囲の地形DEM(国土地理院タイル)から方位角ごとの地平線仰角プロファイルを計算。実装済み・妥当性確認済み。検証済み7地点(下記参照)のみ計算済みで、候補地点全件へのバッチ計算は未着手(「今後の課題」参照)。
  - ステップ3(`step3_light_pollution/`): 観測地点の光害レベル(人工夜空光害・ボートルスケール近似)を取得。実装済み・妥当性確認済み。当初依頼のあったFalchi et al. 2016原本データおよびNOAA/EOG VIIRSデータはいずれも認証必須で取得不可だったため、代替データソース(David Lorenz "Light Pollution Atlas")を使用している。詳細は下記セクション参照。
  - ステップ4(`step4_moonlight/`): 月明かりによる空の明るさ増加モデル。Krisciunas & Schaefer (1991)の天頂輝度モデルを実装(`moon_brightness.py`)し、Skyfieldで月の高度・位相角を計算(`moon_position.py`)。石廊崎(ステップ3で光害由来の背景輝度21.73 mag/arcsec^2と算出済み)を例に、新月夜(2026-06-15)と満月夜(2026-06-30)で実効的な空の明るさがどう悪化するかを比較検証済み(`run_verification.py`)。ステップ3の光害データが天頂輝度のみを扱うため、本モデルも評価点を天頂に固定した簡易版。
  - ステップ5(`step5_cloud_forecast/`): 雲量予報の取得と標高考慮。Open-Meteo APIで雲量予報を取得し、直近はJMA MSM(高解像度・短期)、それ以降はECMWF IFS025(粗い・長期)を使い分け(`cloud_forecast.py`)。「視界遮蔽雲量」(観測者より上の雲=見上げた空の覆われ方)と「雲海指数」(観測者より下の雲)を算出(`elevation_cloud.py`)。2026-09-13の再設計後は、モデル本来の全層/下層/中層/上層雲量(`cloud_cover`系)を主指標とし、気圧面別雲量(1000〜300hPaの10面、各面の高度は毎時のジオポテンシャル高度)は観測者より下の雲を差し引く用途に限定する「見上げ型ハイブリッド」(経緯・検証は下記の専用セクションと「観測者に寄り添う」セクション参照)。エリア代表点(areas.jsonの7エリア)による粗い事前スクリーニングで悪天候エリア×夜を丸ごとスキップする仕組み(`area_prescreen.py`)と、気象格子セル単位でAPI呼び出しを共有するキャッシュ(`grid_cache.py`、JMA MSM=緯度0.05°×経度0.0625°、ECMWF=0.25°×0.25°)を実装済み。
  - ステップ6(`step6_viewpoint_survey/`): 候補地点データの網羅性検証。Overpass API(認証不要)で関東地方(bbox: 緯度34.5〜37.0、経度138.0〜141.0)の`tourism=viewpoint`タグ付きノードを取得(`fetch_viewpoints.py`)。1826件取得し、うち名前(name タグ)ありは754件(`viewpoints_kanto.json`)。**この方法はOSM上の「展望が良い場所」を機械的に収集したもので、星空・星景撮影への適合性は保証しない**(今後の主軸との違いは次のセクション参照)。
  - ステップ7(`step7_geocoding/`): 候補地点の座標確定とDB化。
    - 逆ジオコーディング(座標→市区町村名、`reverse_geocode.py`)を使い、検証済み7地点+ステップ6のviewpoint(名前ありのうち重複4件削除後の750件)を候補地点DB `candidate_sites.db`(SQLite、計757件)に構築(`build_candidate_db.py`)。Nominatim利用規約(1秒1リクエスト以下・識別可能なUser-Agent・キャッシュ必須)に準拠したレートリミッタを実装。
    - 重複検出(`dedup.py`、近接200m以内または同名類似)で30グループ・51件の削除候補を検出済みだが、**判断案の提示のみで、ユーザー承認・DB適用は未実施**(round1の検証済み地点が絡む4件のみ`apply_dedup.py`で適用済み)。詳細は「今後の課題」参照。
    - 2026-09-06、順ジオコーディング(地名→座標、`forward_geocode_curated.py`)を追加。詳細と経緯は次の「候補地点データの現状」セクション参照。
- 各ステップの完了時には、次のステップに進む前に実現性・妥当性を確認する。

## 候補地点データの現状(2026-09-06時点)と今後の主軸

候補地点データは由来の異なる2系統(下記1・2)を統合したマスターファイル(3)を新設した。

1. **`step7_geocoding/candidate_sites.db`**: OSM `tourism=viewpoint`タグの機械的収集(ステップ6・7)由来、757件(検証済み7地点+viewpoint 750件)。地理的網羅性はあるが、タグは一般的な「眺めが良い場所」を指すのみで、星空・星景撮影に適した光害・地平線条件を満たすかは未フィルタ。**このファイル自体は削除・変更していない**(統合はコピー・参照のみ)。
2. **`curated_spots.json`**(プロジェクトルート): 星空・星景撮影の専門家・愛好家による情報発信サイトや自治体観光サイト等をWeb調査し、実際に「星空撮影の名所」として紹介されている関東甲信越・中部地方(16都道府県)の候補地99件について、Nominatim順ジオコーディング(地名→座標)を行い、都道府県・市区町村レベルの機械的突合検証まで通過した**84件**+検証未通過の**要手動確認15件**をまとめたもの。生成過程は`step7_geocoding/curated_spots_source.json`→`step7_geocoding/forward_geocode_curated.py`(順ジオコーディング、Nominatim利用規約対応のレートリミッタ・User-Agentを`reverse_geocode.py`と共有)→`step7_geocoding/curated_spots_geocoded.json`。検証の過程で「里美牧場」が無関係施設への誤マッチ、「中間平緑地公園」など数件で調査時点の市区町村記載の誤りが判明し修正済み。
3. **`stargazing_spots.json`**(プロジェクトルート、2026-09-06新設): 1と2を統合した**候補地点マスターファイル**。2(curated 84件)を主軸の`spots`配列とし、1(candidate_sites.db 757件)との間で300m〜1.5km以内の近接ペアを距離だけでなく名称の意味的妥当性も踏まえて目視判断し、**同一地点と判断した6組のみ統合**(それ以外の12組は「同じ広域・別施設」と判断し統合しなかった。生データは`step7_geocoding/cross_dup_candidates_raw.json`参照)。統合6組の内訳と採用座標:
   - 精進湖 他手合浜・石廊崎・ヘブンスそのはら(いずれも検証済みtest_site側と名称一致): **検証済みtest_site座標を採用**し、地形地平線プロファイル(`horizon_profile`)・光害データ計算済みフラグ(`light_pollution_computed`)を引き継いだ。特に「ヘブンスそのはら」はcurated側のNominatim座標(山麓駅と推定、3.2km差)ではなく、検証済みtest_site座標(山頂展望台、地形地平線データあり)をユーザー確認の上で採用している。
   - 奥多摩湖ダムサイトパーキング・山中湖パノラマ台・美し森展望台(いずれもviewpoint側と14.5m以内): curated(Nominatim)座標を維持(viewpoint側に追加データが無いため)。
   - 統合されなかったcandidate_sites.db由来の残り751件(viewpoint 747件+test_site 4件: 富士山頂・須走口五合目・八ヶ岳南麓天文台・いすみ鉄道踏切)は`viewpoint_reference`配列に**補助的な参考データ**として同梱(星空適性は未フィルタである旨を明示)。2の`pending_manual_geocoding`(要手動確認15件)もそのまま引き継いでいる。
   各エントリは`origin`(`curated` / `curated+test_site` / `curated+viewpoint` / `viewpoint` / `test_site` / `curated_external`)、`horizon_profile`(パスまたはnull)、`light_pollution_computed`(bool)で由来と付随データの有無を判別できる。
4. **外部システム由来のスポットリストのマージ(2026-09-07)**: ユーザーが別システムで収集した「場所名+座標」のみのリスト100件(`external_spots_20260907.csv`)を、`check_external_spots_dedup.py`で既存`spots`(84件)+`viewpoint_reference`(751件)と座標ベース(1.5km以内)+名前一致(完全一致/部分文字列包含、`step7_geocoding/dedup.py`の`_names_match`を再利用)で突合した。高確度重複25件は自動スキップ、残り75件は目視判断が必要と判定し、ユーザーに一覧を提示して確認を得た(重複と判断した13件はスキップ、別地点と判断した12件は追加、判断が割れなかった50件はそのまま追加)。採用した**62件**を`merge_external_spots_20260907.py`で逆ジオコーディング(市区町村・都道府県)・標高取得(GSI DEMタイル)・光害計算(LPI/mpsas/bortle_approx/site_type)のうえ`spots`配列に追加した(新設origin=`curated_external`、`ORIGIN_LABELS`/`PRIMARY_ORIGINS`に追加済み)。curated(Web調査による説明文あり)と区別するため、`description`は`null`のまま(出所となる説明文が無いため)。追加後の`spots`合計146件(dark_sky 141件/night_view 5件)。実行前に`stargazing_spots.json.bak_before_external_merge_*.json`としてバックアップを取得済み。
   - **副産物のバグ発見**: `reverse_geocode.py`の`_MUNICIPALITY_KEYS`優先順位(`city`>`town`>`village`...)が、東京都の山間部(奥多摩町・檜原村)では逆転して機能しないことが判明した。Nominatimがこれらの地域では実際の市区町村を`town`/`village`キーに、`東京都`自体を`city`キーに入れて返すため、`city`優先の現ロジックでは市区町村が「東京都」に誤認識される。今回追加した2件(奥多摩周遊道路・月夜見第一駐車場、檜原都民の森)は手動で`prefecture`/`municipality`を修正したが、**既存の757件(`candidate_sites.db`)側で同様の誤りが無いか未検証**。東京都西部の山間地点(奥多摩町・檜原村・桧原村エリア)を扱う際は要注意。
   - また逆ジオコーディングの都道府県取得で、日本の住所は`state`キーではなく`province`キーに入ることが判明した(`fix_curated_external_prefecture_20260907.py`で修正)。`reverse_geocode.py`の`GeocodeResult`は`municipality`のみ返し`raw_address`を呼び出し側に渡す設計のため、都道府県が必要な場合は呼び出し側で`raw_address.get("province")`を参照すること。
   - **`requires_lift_access`の付与漏れ(2026-09-07)**: マージ時、`merge_external_spots_20260907.py`は`requires_lift_access`フィールド自体を設定していなかった(未設定時は`_build_site()`で`False`扱い)。このため千畳敷カール(駒ヶ岳ロープウェイでのみアクセス可)・日光白根山ロープウェイ山頂駅(名称通りロープウェイ必須)・上高地（河童橋）(マイカー規制、シャトルバス/タクシーのみ)が誤って「気軽コース」のTOP10に入った。ユーザーの指摘で発覚し、この3件に`requires_lift_access: true`と`requires_lift_access_note`を手動追加して修正した。**新規追加した62件のうち、他にも同様の交通規制がある地点(例: 乗鞍岳スカイライン・平湯峠、伊吹山ドライブウェイ山頂駐車場は要確認)が残っている可能性があり、全件のレビューは未実施。**

**今後の候補地点収集の主軸は2・3・4(curated/curated_external由来、星空撮影の専門家・愛好家による集合知ベースまたは外部システム収集)に移す。** 1(OSM viewpoint)は星空適性を保証しない機械的収集であるため、`stargazing_spots.json`内では`viewpoint_reference`として補助的な位置づけに変更し、新規の候補地点探索はWeb調査(集合知)ベースまたは同種の外部リスト取り込みで拡充していく。

**(2026-09-07時点で対応済み)**: 探索モード本体`explore_hoshibiyori.py`の`load_candidate_sites()`は、当初`candidate_sites.db`のみを読み込んでいたが、現在は`stargazing_spots.json`(`spots`配列を主、`viewpoint_reference`を補助)を読み込むよう切り替え済み。`generate_report.py`もこれを経由するため、レポート生成は既に本ファイルが対象になっている。

**「夜景系」スポットの分類タグ追加(2026-09-07)**: ユーザーから神奈川県内の候補地点の状況を尋ねられ回答する中で、「菜の花台は夜景スポットとして有名だが星空スポットとも呼ばれている、この辺の評価が難しい」との指摘があった。curated_spots.jsonはWeb調査(星空撮影の専門家・愛好家サイト)で「星空スポット」と紹介されている地点を光害データを見ずに収集しているため、実際には夜景で有名な高台(光害が高く、天の川のような淡い対象には本来向かない)が「星空スポット」として混入している。両者を機械的に見分けられるよう、`classify_site_darkness.py`を追加し、`stargazing_spots.json`の`spots`配列(curated由来84件)全件にLight Pollution Atlas由来の`lpi`/`mpsas`/`bortle_approx`と、`explore_hoshibiyori.py`のLPI_THRESHOLDと同じ基準で分類した`site_type`(`dark_sky`=暗い空・淡い対象向け/`night_view`=夜景系・光害あり)を計算・付与した。`classify_site_darkness.py`は閾値を独自定義せず`explore_hoshibiyori.py`から`LPI_THRESHOLD`をimportして使う(重複定義による将来の食い違いを防ぐため)。`load_candidate_sites()`の`_build_site()`にも`site_type`/`lpi_precomputed`を追加し、パイプライン側からも参照できるようにした。ただし現状は`explore_hoshibiyori.py`のスコアリング・レポート表示ロジックには組み込んでおらず(既存の`stage3_direction_filter`等は引き続きlight_pollution.pyから都度ライブ計算したLPIで足切りしており、この事前計算値はまだ使っていない)、地点データを閲覧・問い合わせる際の参考情報としての追加にとどまる。実行前に`stargazing_spots.json.bak_before_darkness_*.json`としてバックアップを取得済み。

**LPI_THRESHOLDの実地経験による引き上げ(2026-09-07、3.0→5.0)**: 上記の分類タグ追加を受け、ユーザーから「このLPIのワーストリストの中で、神磯の鳥居と安房埼灯台で天の川を撮影することができました」との実地報告があった。撮影方法を確認したところ単発長時間露光(ISO3200・20秒程度、赤道儀や比較明合成等の特別な機材は不使用)とのことで、特別な技術を要さない標準的な方法でLPI 4.93(神磯の鳥居)まで撮影可能であることが判明した。さらに「高尾山や菜の花台の場合は天の川などの撮影は難しく、特に明るい天体を中心とした星空を見に行くという目的のような気がしている」との補足があり、成功実績(神磯の鳥居LPI4.93・安房埼灯台LPI3.27)と失敗経験(菜の花台LPI6.49・高尾山LPI10.18)の間に閾値を置くのが妥当と判断し、`LPI_THRESHOLD`を3.0から5.0に引き上げた(`explore_hoshibiyori.py`)。スコア計算式には`- LPI×10.0`という比例減点項が別途あるため、閾値を緩めても光害の強い地点が上位に来すぎる心配は小さい。反映後、`classify_site_darkness.py`を再実行して`site_type`を再分類(dark_sky 74→82件、night_view 10→2件[高尾山・菜の花台のみ])、`generate_report.py`を再実行してレポート全体に反映した(方角フィルタ通過数43件→48件に増加)。この閾値はまだ限られた実地データ(成功2件・失敗2件相当)に基づく暫定値であり、他地点での成功/失敗例が増えれば再調整の余地がある。

**追加の確認データ(2026-09-07)**: 千葉県内のcurated地点の光害一覧を提示した際、ユーザーから「野島埼灯台(LPI 1.12)、月の沙漠記念公園(LPI 1.92)などで天の川の撮影ができることを確認しました」との報告があった。両地点とも当初の閾値(3.0)より暗く、dark_sky判定の妥当性を裏付ける確認データ(閾値の変更は不要)。

**「星空観賞向き」バッジの追加(2026-09-07)**: ユーザーから「『星空が期待できる』と『星空撮影に適する』をユーザが見分ける方法あるかな?」との相談があり、両者を別バッジ(別セクション)として併記する案を提案・承認され実装した。夜景系(`site_type=="night_view"`、現状は高尾山・菜の花台)は光害LPIが`LPI_THRESHOLD`を超えるため引き続き気軽/硬派コースのランキングからは除外されるが、`generate_report.py`の`main()`で`stage2_cloud_filter()`の出力(`survivors`、LPI/地形の足切り前)から`site_type=="night_view"`のものだけを抜き出し、地点ごとに最も晴れる夜1件を選んで「🌃 星空観賞向きスポット(天の川撮影には不向き)」という別枠セクション(`render_night_view_group()`)に表示するようにした。これにより「天の川撮影には向かないが、晴れていれば星や夜景を楽しむには十分」という情報が失われずに残る。表示位置は「直近・行動用」セクション(気軽/硬派コースの下)。ランキングスコアは付けず、晴れ間の時間帯・視界遮蔽雲量・光害LPIのみを表示する簡易カード形式。

## 今後の課題

- **`generate_report.py`の実行時間が候補地点数に応じて増大**: 2026-09-07の外部リストマージでspots配列が84件→146件に増えたところ、実行時間が約5分→約21分に伸びた(API呼び出し自体はグリッドセルキャッシュのおかげで大きくは増えていない[159回]。増加分の大半は`stage_outlook`等での純粋なSkyfield天文計算[天体位置・薄明時刻の`find_discrete`呼び出しなど]がサイト数×夜数に比例して増えるため)。今後さらに候補地点を追加する場合、実行時間がボトルネックになる可能性がある。対策候補: 天文計算のキャッシュ化(地点の緯度がほぼ同じなら天文条件も酷似するため近傍地点でまとめ計算する等)、並列化、またはエリア代表点による事前スクリーニング(`area_prescreen.py`は雲量取得の粗い事前フィルタとして実装済みだが天文計算自体には使われていない)の拡張。
- **`reverse_geocode.py`の東京都山間部での市区町村誤認識バグ**: 上記「候補地点データの現状」の4番目の項目参照。`_MUNICIPALITY_KEYS`の優先順位(`city`>`town`>`village`)が、Nominatimが東京都山間部(奥多摩町・檜原村等)で`city`キーに「東京都」自体を返すケースで逆効果になる。今回追加した2件は手動修正したが、既存の757件(`candidate_sites.db`)側で同様の誤りが無いか未検証。
- **重複判定30グループ(51件)が承認待ち**: `step7_geocoding/dedup_report2.txt`・`pending_deletions_round2.json`に判断案あり。round1(検証済み地点が絡む4件)のみ`apply_dedup.py`で適用済みで、残る30グループは削除の実行前にユーザーの目視確認が必要。これは`candidate_sites.db`内部(viewpoint同士)の重複判定であり、上記の`curated_spots.json`↔`candidate_sites.db`間のクロス重複統合(対応済み)とは別の課題。
- **地形地平線プロファイルの全件バッチ計算が未着手**: `step2_terrain/horizon_profile.py`による地平線プロファイル計算は検証済み7地点のみ実施済み。`explore_hoshibiyori.py`は地形データの無い地点について地形チェックをスキップし「地形データなし」と明示する暫定対応のままになっている。`stargazing_spots.json`の`spots`84件のうち地形地平線データがあるのは統合済みの3件(精進湖 他手合浜・石廊崎・ヘブンスそのはら)のみで、残り81件を含む候補地点全件への本格適用にはバッチ計算の実施が必要。
- **`stargazing_spots.json`の要手動確認15件の座標埋め**: さくら宇宙公園・高戸小浜海岸・星野河川公園・ぐんま天文台・三宅島灯台・乙女高原・平沢峠・高峰高原・妙高サンシャインランド・光ヶ原高原・スターランドさかだに・道の駅池田はっぴぃ農園・大平御嶽展望台・面ノ木峠・里美牧場。Nominatimに該当データがなく自動ジオコーディングでは特定できなかったため、Googleマップ等での手動確認が必要(`pending_manual_geocoding`配列に地名・特徴つきで保持)。
- **統合しなかった12組の再検討**: `step7_geocoding/cross_dup_candidates_raw.json`に記録した「同じ広域・別施設」と判断した12組(戦場ヶ原、湯ノ湖、羊山公園、田貫湖、爪木崎、栃本広場など)のうち、田貫湖(305m)・爪木崎(190m)・栃本広場(95m)はやや近く、判断が割れる可能性がある。必要に応じて再判断する。
- **`stargazing_spots.json`の対象地域拡張が未着手**: 現状は関東甲信越・中部地方(16都道府県)のみ。他地域(東北・関西以西など)への拡張は今後の検討事項。
- ステップ5の視界遮蔽雲量に残る制約(方角情報なし・層区分固定、「標高考慮の雲量判定」セクション末尾参照)、ステップ2・3で使用する外部データ(GSI DEMタイル・Light Pollution Atlas)の利用規約に関する公開前の確認事項(それぞれ下記の専用セクション参照)は、いずれも未対応のまま継続中。

## 使用予定の主なライブラリ

- **astropy** または **Skyfield** — 天文計算（ステップ1で使用）
- **rasterio** — インストール自体は可能(GDAL同梱wheelあり)だが、ステップ3では光害データがバイナリタイル形式(GeoTIFFではない)のため未使用。ローカルのGeoTIFF/ラスタファイルを直接扱う段になったら使用する。
- **geopandas** — 未導入。地理ベクタデータを扱う段になったら導入する。
- 測地線計算（緯度経度+方位角+距離 → 到達点）は、この環境（Python 3.15 RC）で `pyproj` のビルドに失敗したため、`step2_terrain/geodesy.py` にVincenty法を自前実装している。将来 `pyproj` が使えるようになった場合は置き換えを検討してよいが、必須ではない。

## DEM（標高）データ取得に関する注意 — 公開時の留意点

ステップ2の地平線プロファイル計算では、国土地理院の「地理院タイル（標高タイル）」
（`cyberjapandata.gsi.go.jp`、ログイン・APIキー不要）を取得元として使用している
（`step2_terrain/gsi_dem.py`）。**このプロジェクトを一般公開する前に、必ず以下を確認・対応すること。**

- **出典表示は必須**: 国土地理院コンテンツ利用規約に基づき、この標高データを利用した成果物・画面には
  「出典：国土地理院」等の表示を行うこと。データを加工している場合はその旨も明記する。
  最新の利用条件は公開前に国土地理院の利用規約ページで必ず再確認すること（URLはこのファイルに記載しない。検索して一次情報を確認する）。
- **アクセス頻度への配慮**: このタイル配信は無償・無認証で提供されている公共インフラであり、
  想定利用は主に地図表示（ブラウザ等からの通常の閲覧アクセス）である。バッチ処理での
  大量取得は望ましくなく、実装上は次の対策を必ず維持すること。
  - タイル単位のディスクキャッシュ（`step2_terrain/gsi_dem.py` の `DemTileStore`）を無効化しない。同一タイルを重複取得しない設計を崩さないこと。
  - 新規タイル取得時の小休止（ポライトネス・ディレイ）を削除・大幅短縮しない。
  - 識別可能な User-Agent を送信し続けること（何のツールからのアクセスかを国土地理院側が判別できるようにするため）。
- **サービス化する場合の追加検討事項**: 個人利用のスクリプトから、複数ユーザーが使う公開サービスに
  発展させる場合、ユーザーごとのリクエストが積み重なって同サービスへの負荷が無視できなくなる可能性がある。
  公開前に以下を検討すること。
  - サーバー側で共有DEMタイルキャッシュ（全ユーザー共通）を持ち、ユーザー数に比例してGSIへのリクエストが増えない構成にする。
  - 対象エリアを絞れるなら、事前に必要範囲のDEMをまとめて取得・自前ホスティング（オフラインDEM化）する方式へ切り替える。
  - 国土地理院の最新の利用規約・注意事項（大量アクセスに関する記載の有無）を公開前に必ず確認する。

## 光害データ取得に関する注意 — 公開時の留意点(要対応)

ステップ3の光害レベル取得では、当初依頼されたFalchi et al. 2016 "World Atlas of
Artificial Night Sky Brightness" の原本データと、代替候補のNOAA/EOG VIIRS年次
コンポジットの両方を実際に調査した結果、**いずれも現在は認証なしで取得できない**
ことを確認した(GFZ Potsdam Data Servicesは著者への利用申請フォーム必須、
eogdata.mines.eduは全ディレクトリがEOGアカウントのOpenID Connectログイン必須)。

このため、代替として **David Lorenz氏が個人運営する "Light Pollution Atlas"**
(`https://djlorenz.github.io/astronomy/lp/`)を採用した(`step3_light_pollution/light_pollution.py`)。
Cinzano/Falchiと同じ手法をNOAA/EOG VIIRSの最新データで著者が独自に再計算したもので、
サイトの地図(overlay/dark.html)がクリック時に読み込む生バイナリタイル
(`binary_tiles/{year}/binary_tile_{tilex}_{tiley}.dat.gz`、GitHub Pages配信・認証不要)
を直接利用している。**このプロジェクトを一般公開する前に、必ず以下を確認・対応すること。**

- **ライセンス表記が見当たらない**: サイト内を確認したが、CCライセンス等の明示的な
  利用許諾表示や「Used with permission」等の記載は見つからなかった。著者
  (David Lorenz, dlorenz@wisc.edu)による正式な利用規約は存在しない。
  個人の検証目的の低頻度アクセス(本プロジェクトの現状)は、サイト自体が
  同等の機能(地図クリックでの地点参照)を一般公開していることから問題ない
  可能性が高いが、**一般公開するサービスに組み込む前に、著者に利用可否を
  メールで確認すること。** 出典表示(`ATTRIBUTION`定数、サイトURL明記)は
  最低限のマナーとして継続する。
- **ボートルスケール変換は非公式の簡易近似**: 変換元データ提供者自身が
  「天頂輝度とボートルスケールを単純に対応させるべきではない」と明言している
  (`bortle.html`)。`step3_light_pollution/light_pollution.py` の
  `bortle_from_ratio()` はあくまで目安であり、公式の変換ではないことを
  UI等に表示する場合も明記すること。
- **アクセス頻度への配慮**: GSIタイルの場合と同様、タイル単位のディスクキャッシュ
  (`LightPollutionTileStore`)を無効化しないこと。GitHub Pages配信のため
  GSIほど負荷に敏感ではないと考えられるが、同様の配慮を維持する。
- **データの限界**: このデータは「新しい衛星データによる再計算」であり、著者自身が
  「旧来のアトラス(2015年版)は地方部で明るさを過小評価しがちだった」と指摘している。
  そのため本データでの数値は、旧アトラスを前提とした一般的な「ボートル3〜4程度」という
  感覚的な期待値より高め(明るめ)に出ることがある。これはバグではなくデータ更新による
  補正であることを、検証結果を解釈する際に留意すること。

## 探索モードの「見頃」時間帯算出における天文薄明の考慮(2026-09-07修正)

`explore_hoshibiyori.py`の第1段階(月フィルタ)は、当初「見頃」時間帯の両端を
日没・日の出(太陽高度0°)としていた。しかし日没直後・日の出直前は市民薄明・航海薄明・
天文薄明でまだ空が明るく、天の川等は実際には見えない。この状態でレポートの「見頃」を
見たユーザーから「日没直後(17:59など)はまだ明るい気がする」との指摘があり、
修正した(**このユーザー観察がきっかけで発覚・修正**)。

修正後は`find_astro_dark_events()`(Skyfieldの`almanac.dark_twilight_day`を利用)で
天文薄明終了(太陽高度-18°を下回る時刻)〜天文薄明開始(-18°を上回る時刻)を求め、
この区間を月フィルタ・雲取得・「見頃」表示の基準として使う(`NightCandidate`に
`dark_start`/`dark_end`フィールドを追加)。従来の日没・日の出(`sunset`/`sunrise`)は
月相アイコン算出用の代表時刻や、月の有無に左右されない一般的な「見どころ」ガイド
(`generate_report.py`の`build_sky_guide()`、こちらも2026-09-07に天文薄明基準に統一)
の対象範囲としては引き続き使用している。

**地点ごとの計算への切り替え(2026-09-07、同日追加修正)**: 上記の天文薄明修正の直後、
ユーザーから「気軽/硬派コースTOP10の候補地点が(遠く離れているのに)みな全く同じ「見頃」
時刻になっているのはおかしいのでは、雲量は考慮されているのか」との指摘があり確認したところ、
「見頃」区間(`night.window_start`/`window_end`)が石廊崎1地点の座標だけを使って夜ごとに
1回計算され、全候補地点(84件)で使い回されていたことが判明した(**これもユーザー観察が
きっかけで発覚**)。修正として`compute_site_night_window()`を新設し、`stage2_cloud_filter()`
(雲量取得範囲)・`stage3_direction_filter()`(「見頃」表示・狙い目時刻)・`stage_outlook()`
(中期見込み)のいずれも、候補地点自身の座標で天文薄明・無月時間帯を計算し直すよう変更した
(例: 2026-09-09夜、石廊崎基準19:26〜03:56に対し白山では19:37〜03:52と、地点ごとに
異なる時刻になることを確認済み)。天文計算のみでAPI呼び出しは発生しないため、
処理時間への影響は軽微。なお、夜ごとにどの日付を候補とするか(MSM/ECMWF圏内の判定含む)
自体は引き続き石廊崎基準の粗い判定のままで、これは意図的な設計(候補日選定は地点非依存の
大まかなスクリーニングでよいため)。

**雲量による時間帯の絞り込み(2026-09-07、同日3件目の修正)**: 上記の地点ごと計算修正の
直後、ユーザーから「雲量による時間帯の絞り込みをお願いします。重要な仕組みです」との
依頼があり対応した。従来は`stage2_cloud_filter()`が天文条件で決まる区間全体
([window_start, window_end])の**平均**視界遮蔽雲量を閾値(30%)と比べるだけで、
「見頃」として表示する時間帯自体は雲の時間変化を反映していなかった(区間の前半だけ
晴れて後半は曇っていても、平均さえ閾値以下なら区間全体をそのまま「見頃」と表示していた)。
修正として`find_longest_clear_window()`を新設し、時間別の視界遮蔽雲量データから
「閾値(30%)以下が連続`MIN_CLEAR_WINDOW_HOURS`(=1.0)時間以上続く区間」のうち最長のものを
探し、見つかった晴れ間そのものを「見頃」(`window_start`/`window_end`)として表示・
以降の方角フィルタ・スコアリングに使うよう変更した。晴れ間が1時間未満しかない
組み合わせは候補から除外される(足切り基準が「区間全体の平均」から「連続した晴れ間の
有無」に変わったことで、通過する組み合わせ数は変化する。2026-09-09時点の検証では
45組→64組に増加。区間全体では平均が閾値を超えていたが、一部に十分な晴れ間がある
組み合わせが新たに拾えるようになったため)。`FinalCandidate`に`clear_hours`
(晴れ間の長さ、時間)フィールドを追加し、スコア計算の時間項も`moon_free_hours`
(天文条件のみの無月時間帯)から`clear_hours`(雲も考慮した実際の晴れ間長)に置き換えた。

なお、中期見込み(`stage_outlook()`、ECMWFカレンダー)側は元々「雲量は参考の晴天率のみに
使い、足切り・スコアには使わない」という意図的な設計(長期予報は精度が低く、時間帯単位の
足切りをすると却って過信を招くため)なので、今回はそのままにしている。

**スコア計算式の既知の癖**: 近日中(`FinalCandidate.score`、MSM圏内)は
`(100-視界遮蔽雲量%)×1.0 + max(0,天体高度-地形地平線高度)×0.5 + clear_hours×3.0
- 光害LPI×10.0`という式で、雲の良さを「区間の平均視界遮蔽雲量(雲量項)」と
「晴れ間の長さ(clear_hours項)」の両方で二重に評価している(意図的な重み付けとして
残しているが、雲の影響が強すぎると感じる場合は調整の余地がある)。また中期見込み
(`OutlookCandidate.score`)は光害項の書き方が`100-LPI×10`という基準点込みの形になっており
近日中の式と算出方法が異なるため、両者のスコアの絶対値は単純比較できない(2026-09-07、
ユーザーからのスコア計算式に関する質問がきっかけで整理・記録)。

## 標高考慮の雲量判定(sky_obstruction_cloud_cover)の設計と経緯

`step5_cloud_forecast/elevation_cloud.py` が、時刻ごとに「視界遮蔽雲量」(観測者が見上げた空の
うち雲に覆われる割合、%)と「雲海指数」(観測者より下に広がる雲、%)を算出する。
`explore_hoshibiyori.py`・`generate_report.py`・`area_prescreen.py`はいずれも
`get_level_cloud_forecast()`→`hourly_sky_obstruction_and_cloud_sea()`経由でこの値を使う。

### 現行設計: 見上げ型ハイブリッド(2026-09-13)

- **主指標はモデル本来の雲量**: Open-Meteoの`cloud_cover`(全層)・`cloud_cover_low/mid/high`
  (下層0〜3km/中層3〜8km/上層8km以上、海面からの高度。公式ドキュメントで確認)。JMA MSM・
  ECMWF IFSともモデルが直接出力する値で、層の重なりもモデル自身が考慮済み。
- **気圧面別雲量(`cloud_cover_XXXhPa`、1000/925/900/850/800/700/600/500/400/300hPa)+各面の
  ジオポテンシャル高度は「観測者より下の雲を差し引く」ためだけに使う**: 下層/中層/上層の各層に
  属する気圧面の(近似)雲量を重みに、「層内の雲のうち観測者より上にある割合」w(0〜1)を求め、
  視界遮蔽雲量 = 全層雲量 × (上の分の3層合成 ÷ 元の3層合成)、雲海指数 = 下の分の3層合成とする。
  低標高地点では全w=1となり**視界遮蔽雲量はモデル本来の全層雲量に一致**する(検証: 石廊崎・
  野島埼灯台・神磯の鳥居でMSM圏内72時間中68〜70時間が完全一致、残りも差0.2pt以内)。
  高所では観測者より下の雲が抜けた分だけ下がる(同検証: ヘブンスそのはら1600mで平均−6pt・
  雲海指数18%、千畳敷カール2612mで−17pt・42%、富士山頂3776mで−23pt・51%)。
- **層の合成はmaximum-random overlap**(隣接して雲のある層はmax=同じ雲塊、雲の無い層で
  隔てられた塊同士はランダム重ね合わせ)。`max_random_overlap()`。
- モデル本来の雲量が欠損している時刻だけ、気圧面別雲量のみからの合成(max-random)に
  フォールバックする(`_weighted_average()`、関数名は後方互換)。
- 気圧面の高度は毎時のジオポテンシャル高度(実高度)。標準大気値との差は1000hPaで+10〜80m、
  850hPaで+70〜140m、300hPaで+500〜650m(2026-09-13ライブ確認)。それだけがnullの面は
  標準大気値にフォールバック。面・変数ごとに「MSMの値があればMSM、なければECMWF」で埋め、
  `HourlyLevelCloud.source`は過半数を供給したモデル名(`forecast_regime_for_night`の多数決に使用)。
  ecmwf_ifs025は900/800hPaをnullで返すため、ECMWF圏内は8面で処理される。
- 観測者が気圧面の中心高度以下にいるときはその面の雲を割り引かない(`_above_weight()`)。
  地表付近の観測者は「1000hPa(約100〜200m)の霧・層雲」を全て頭上に見るため。
- **API要求変数**は`level_hourly_vars()`に一元化(本来の雲量4変数+気圧面雲量10面+高度10面)。
  変数文字列がキャッシュキーの一部なので、呼び出し側で独自に組み立てないこと。

### 経緯(時系列)

1. **2026-09-06**: 500hPaまでしか見ておらず上層雲(巻雲)を丸ごと見落とす不具合(弥彦山、
   全層89〜99%相当の夜を13%と誤判定)を300hPaまで拡張して修正。250hPa以上はjma_msmが
   nullのため含めない。
2. **2026-09-13(朝)、yamabiyoriとの同期**: ユーザーの依頼でyamabiyori側(2026-09-09以降)の
   改善を取り込んだ(「プロジェクトの独立性」の例外記録参照)。事前確認では、Open-Meteo取得の
   骨格(2モデル別取得・時刻ごとMSM優先結合・3回リトライ・3時間TTLディスクキャッシュ)は同じで、
   hoshibiyori側にしかない機能(格子セル単位のAPI共有、300hPaまでの上層雲、ECMWF側6時間TTL、
   取得失敗時にNoneを返してセル単位スキップ、雲海指数、エリア事前スクリーニング)はyamabiyoriに
   無いため維持。取り込んだのは「ジオポテンシャル高度」「面ごとの欠損許容」「900/800hPa追加」
   の3点。この時点では従来の「気圧面別雲量の全層ランダム重ね合わせ」を維持しており、
   `generate_report.py`の結果は雲フィルタ通過171/438組(`run_report_gph_levels.log`)。
3. **2026-09-13(同日)、星空撮影者目線での点検と再設計**: ユーザーから「hoshibiyoriは地表から
   星を見上げるため、登山者用yamabiyoriと雲の見方・評価方法が異なる。取り扱いが正しいか確認して」
   と指摘。低標高6地点×MSM圏内72時間(414時間)でモデル本来の全層雲量と比較した結果:

   | 合成方式(標高0m相当) | 平均バイアス | 平均絶対差 | 30%閾値で誤って曇り判定 |
   |---|---|---|---|
   | 気圧面10面の全層ランダム重ね合わせ(手順2時点) | +15.6pt | 20.4 | 46時間/414 |
   | 気圧面10面のmax-random | −0.4pt | 14.9 | 30時間 |
   | 本来の下層/中層/上層のランダム重ね合わせ | +11.4pt | 11.4 | 45時間 |

   原因は、925/900/850hPaにまたがる1枚の下層雲を独立3層として重ね合わせていたこと
   (例: 神磯の鳥居9/13 16時、本来46%[下層46・中層35・上層0]に対し96%)。手順2の900/800hPa
   追加はこの多重計上を強めていた(一方で925→850hPa間の雲を見落としていた旧来の欠陥も本物で、
   精進湖9/14 04時の例では900hPa面の雲量49%を旧ロジックが見落としていた)。上層雲>70%の
   112時間で見逃しは0(上記1の修正は有効)。これらを踏まえ、モデル本来の雲量を主指標とする
   現行設計に再構成した。実装後に`generate_report.py`を再実行し前後比較(`run_report_hybrid_lookup.log`、
   結果: 雲フィルタ通過が171組→**196組**/438組(同じ3夜・146地点、予報ラン更新の影響は数十分差で
   小さい)、方角フィルタ通過122件→149件。過大評価で落ちていた「モデル本来では半分程度晴れ」の
   組み合わせが拾えるようになった。所要13.7分(通信206回、新変数キーでキャッシュが冷えた状態)。
   実装前のレポートは比較用にscratchpadへ退避したのみで、リポジトリ側は上書き済み。

### なお残る制約

- 視界遮蔽雲量は地点(MSM約5km格子/ECMWF約25km格子)の全天平均で、対象天体の方角に雲があるか
  どうかは分からない(点予報の限界)。
- 下層/中層/上層の高度区分(3km/8km)は固定で、観測者が層の内部にいる場合の切り分けは気圧面の
  近似雲量の分布に依存する(気圧面に雲が無ければ幾何的な割合で代用)。
- 中期見込み(ECMWF圏内)は引き続き雲量を足切りに使わず晴天率の参考表示のみ(意図的な設計)。
- ステップ2・3で使用する外部データ(GSI DEMタイル・Light Pollution Atlas)の利用規約に関する
  公開前の確認事項は別セクション参照。
